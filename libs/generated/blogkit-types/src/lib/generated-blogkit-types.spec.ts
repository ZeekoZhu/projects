import { generatedBlogkitTypes } from './generated-blogkit-types';

describe('generatedBlogkitTypes', () => {
  it('should work', () => {
    expect(generatedBlogkitTypes()).toEqual('generated-blogkit-types');
  });
});
